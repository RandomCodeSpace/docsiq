import { describe, it } from 'vitest'

// useNotesSearch is exported from ../useNotes.ts, not a standalone file.
// Its tests live in useNotes.test.tsx.
describe.skip('useNotesSearch (folded into useNotes.ts)', () => {
  it('see useNotes.test.tsx', () => {})
})
