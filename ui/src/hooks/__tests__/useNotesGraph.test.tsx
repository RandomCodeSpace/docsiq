import { describe, it } from 'vitest'

// useNotesGraph is exported from ../useNotes.ts, not a standalone file.
// Its tests live in useNotes.test.tsx.
describe.skip('useNotesGraph (folded into useNotes.ts)', () => {
  it('see useNotes.test.tsx', () => {})
})
