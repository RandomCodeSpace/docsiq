import { describe, it } from 'vitest'

// useNotesTree is exported from ../useNotes.ts, not a standalone file.
// Its tests live in useNotes.test.tsx.
describe.skip('useNotesTree (folded into useNotes.ts)', () => {
  it('see useNotes.test.tsx', () => {})
})
